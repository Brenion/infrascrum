import Controller from '@ember/controller';
import { action } from '@ember/object';
import { service } from '@ember/service';
import { tracked } from '@glimmer/tracking';

export default class ProjectsNewprojectController extends Controller {
  // @service store;
  // @service router;
  // @tracked project = {
  //   projectName: '',
  //   startDate: '',
  //   endDate: '',
  //   description: '',
  // };
  // @action async addProject(e) {
  //   console.log(e);
  //   console.log('addproject');
  //   let project = await this.store.createRecord('project', this.e);
  //   console.log(project);
  //   await project.save();
  // //   ici peut etre placer du vide pour on verra
  // this.router.transitionTo('projects');
  //}
}
